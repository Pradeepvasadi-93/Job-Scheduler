# Job-Scheduler
Job Scheduler Dashboard is a full‑stack web application designed to manage and monitor scheduled jobs with a clean, modern UI. The frontend is built with React and Tailwind CSS, while the backend exposes REST APIs for job creation, execution, and deletion.
